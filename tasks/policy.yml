---

- include_tasks: 'import.yml'
  vars:
    facts:
      - '{{ i.terraform.values() }}'
    fact: 0
    r_id: 'arn'
    r_skip:
      - 'description'
      - 'name'
      - 'name_prefix'
    r_type: 'aws_iam_policy'
